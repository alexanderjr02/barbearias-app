<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The INTERNET permission is required for development. Specifically,
         the Flutter tool needs it to communicate with the running application
         to allow setting breakpoints, to provide hot reload, etc.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Debug-only: allow plain-HTTP (cleartext) so the app can reach the dev
         backend over the LAN (http://<ip>:3000) while testing on a physical
         phone. This override applies to debug builds only and never ships in
         release, which stays HTTPS-only. -->
    <application android:usesCleartextTraffic="true" />
</manifest>
